<!DOCTYPE html>
<html lang="id">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Login - Vape Mod</title>
  <link rel="stylesheet" href="/style.css">
</head>
<body>
  <div class="container">
    <div class="vapor-effect"></div>
    <div class="content-box">
      <a href="/" class="back-link">← Kembali</a>

      <h1 class="neon-title">LOGIN</h1>
      <h2 class="subtitle">SQL Injection Vulnerable</h2>

      <% if (error) { %>
        <div class="alert alert-error"><%= error %></div>
      <% } %>

      <% if (success) { %>
        <div class="alert alert-success"><%= success %></div>
      <% } %>

      <form method="POST" action="/login" class="login-form">
        <div class="form-group">
          <label for="email">Email</label>
          <input
            type="text"
            id="email"
            name="email"
            placeholder="admin@vapemod.com"
            required
          >
        </div>

        <div class="form-group">
          <label for="password">Password</label>
          <input
            type="password"
            id="password"
            name="password"
            placeholder="admin123"
            required
          >
        </div>

        <button type="submit" class="btn-submit">LOGIN</button>
      </form>

      <div class="exploit-hint">
        <h4>Hint untuk Testing:</h4>
        <p>Coba gunakan payload SQL Injection pada field email:</p>
        <code>' OR 1=1 --</code>
        <p class="small-text">Query vulnerable: SELECT * FROM users WHERE email = '[input]' AND password = '[input]'</p>
      </div>
    </div>
  </div>
</body>
</html>
